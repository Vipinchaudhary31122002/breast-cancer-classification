services:
  app:
    build:
      context: ..
      dockerfile: docker/dockerfile.dev

    image: breast-cancer-classification:dev

    working_dir: /app

    volumes:
      - ..:/app

    command: sleep infinity